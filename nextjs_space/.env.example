# Database (Required)
# Get a free PostgreSQL database from Neon.tech or Supabase.com
DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"

# NextAuth (Required)
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here"

# Your deployed URL (update after first deploy)
NEXTAUTH_URL="https://your-site.vercel.app"

# Email Notifications (Optional - won't work on Vercel without replacement)
# You'll need to replace with SendGrid, Resend, or similar
# NOTIFICATION_API_KEY="not-applicable-on-vercel"
